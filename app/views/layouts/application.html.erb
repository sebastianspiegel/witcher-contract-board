<!DOCTYPE html>
<html>
  <head>
    <title>WitcherContractBoard</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="header">
    <h1>Digital Witcher Notice Board</h1>
    <div class="nav">
      <% if session[:user_id] %>
        <% if current_user.witcher? %>
          <%= link_to "Available Contracts", contracts_path %> | 
        <% else %>
          <%= link_to "Post New Contract", new_contract_path %> | 
        <% end %>
        <%= link_to "Log Out", logout_path %> | 
        Currently signed in as: <%= link_to current_user.name, user_path(current_user.id) %>
      <% else %>
        <%= link_to "Sign In", signin_path %>
      <% end %>
      <br><br>
    </div>
    </div>
    <div><%= flash[:message] %></div>
    <%= yield %>
  </body>
</html>
