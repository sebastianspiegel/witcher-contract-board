<!--- old form here

    <%= form_for :monster do |f| %>
    <%= f.label :weakness_ids, "Weaknesses:" %>
    <%= f.collection_check_boxes(:weakness_ids, Weakness.all, :id, :name) %>

    Or create new weakness:<br>
    <%= f.fields_for : %>

    <br><%= f.submit "Add Weaknesses" %>
<% end %>
<% end %>
---> 