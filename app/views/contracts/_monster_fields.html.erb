Select a monster:
<%= f.collection_select :monster_id, Monster.all, :id, :name, prompt: true %><br>

Or create a new monster:
<%= f.fields_for :monster do |m| %>
    <%= render partial: "monsters/form", locals: { in_the_partial: m } %>
<% end %>

