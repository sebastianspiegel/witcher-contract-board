<%= f.label :monster %><br>
<%= f.collection_select :monster_id, Monster.all, :id, :name, prompt: true %><br>

<br>Or create a new monster:<br>
    <%= f.fields_for :monster do |m| %> 
        <%= m.label :name %>
        <%= m.text_field :name %>
        <%= m.label :type %>
        <%= m.collection_select :type_id, Type.all, :id, :name, prompt: true %>
    <% end %>


