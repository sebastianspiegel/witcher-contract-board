<h2>Post a new contract</h2>

<% if @contract.errors.any? %>
    <div id="error_explanation">
    <h2><%= pluralize(@contract.errors.count, "error") %> prohibited this from being saved:</h2>

    <ul>
        <% @contract.errors.full_messages.each do |error| %>
        <li><%= error %></li>
        <% end %>
    </ul>
    </div>
<% end %> 

<%= render partial: "form" %>