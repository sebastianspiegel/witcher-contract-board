<h2>Available Contracts</h2>

<% @contracts.each do |contract| %>
   <% if contract.contract_is_claimed? %>
     <%= link_to contract.monster.name, contract_path(contract.id) %><br>
   <% end %>
<% end %>

