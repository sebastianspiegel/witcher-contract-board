<%= f.label :location %><br>
<%= f.collection_select :location_id, Location.all, :id, :name, prompt: true %><br>

<br>Or create new location:<br>
    <%= f.fields_for :location do |l| %>
        <%= l.label :name %>
        <%= l.text_field :name %>
    <% end %>