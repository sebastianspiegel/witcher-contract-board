<%= form_for(@monster) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label(:type) %>
  <%= f.collection_select :type_id, Type.all, :id, :name, prompt: true %>
  <%= f.submit %> 
<% end %>